<%= form_for [current_user, @task] do |f| %>
  <p>
  	<%= f.label "Taskname" %><br />
  	<%= f.text_field :taskname %>
  </p>
  <p>
  	<%= f.label "Description" %><br />
  	<%= f.text_area :description %>
  </p>
  <p class="button"><%= f.submit "Add task" %></p>
<% end %> 